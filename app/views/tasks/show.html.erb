<ul>
    <li><%=  @task.title %></li>
    <li><%= @task.details %></li>
    <li><%= @task.completed %></li>
</ul>

<%= link_to "back to list", tasks_path%>
<%= link_to "edit", task_edit_path%>
<%= link_to "destroy", task_destroy_path(@task), method: :delete,  data: { confirm: "Are you sure?" } %>
