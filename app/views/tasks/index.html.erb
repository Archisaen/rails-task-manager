<h1>Here is the list of all the tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
    <li><%= task.details %></li>
    <li><%= task.completed %></li>
  <% end %>
</ul>

<%= link_to "go to form", new_task_path%>
